/* exported head */
/* look at the index of each element in the array */
/* if the index i strictly equals to 0, which is the index of the first element */
/* return the array at the index i */
/* function head(array) {
  for (var i = 0; i < array.length; i++) {
    if (i === 0) {
      return array[i];
    }
  }
}
*/

function head(array) {
  return array[0];
}
