const converMinutesToSeconds = (minutes) => minutes * 60;
const greet = (name, status = 'good') => `Hey, ${name}, are you ${status}?`;
const getArea = (width, height) => width * height;
const getLastElement = (array) => array[array.length - 1];
