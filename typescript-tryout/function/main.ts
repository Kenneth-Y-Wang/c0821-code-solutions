const converMinutesToSeconds= (minutes: number):number => minutes * 60;

const greet = (name: string, status='good'): string => `Hey, ${name}, are you ${status}?`;

const getArea=(width: number, height:number): number => width*height;

const getLastElement= (array:string[]):string => array[array.length-1];
